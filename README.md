# Python-Workshop
Quick! I need to sharpen up my Python skills!

git skills learned  
git init  
git status  
git add <filename> or . for everything  
git commit -m "message"  
git push  
git branch (check what branch you are on)  
git fetch (remote all branches -> local) / git pull (remote current branch -> local)  
git config --list (for the username and email on the commits)  
git log (for commit history)  
